<?php
/**
 * Module: Enable Debug Mode
 *
 * Debug constants (WP_DEBUG, WP_DEBUG_LOG, WP_DEBUG_DISPLAY) are managed
 * directly in wp-config.php via includes/wp-config-editor.php.
 * WordPress handles ini_set() calls in its own bootstrap based on those constants.
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}
